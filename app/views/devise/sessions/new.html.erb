<div class="sign">
<div class="signlogo">
  <div class="info">
<h1><%= link_to "AccompliShit", root_url %></h1>
<h2>Complete Tasks - Level Up - Rock On</h2>
<h3><%= link_to "What the hell is AccompliShit?", root_url %></h3>
</div>
</div>

<h2>Sign in</h2>

<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <div>
  <%= f.text_field :username, :autofocus => true %><br />
<%= f.label :username %></div>

  <div>
  <%= f.password_field :password %><br />
<%= f.label :password %></div>
<br />
  <% if devise_mapping.rememberable? -%>
    <div class="remember"><%= f.check_box :remember_me %> <%= f.label :remember_me %></div>
  <% end -%>
<br />
  <div><%= f.submit "Sign in" %></div>
<% end %>

<div class="links">
<%= render "devise/shared/links" %>
</div>
</div>
